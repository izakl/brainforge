# Framework release 0.3.0

- Date: 2026-06-25
- Manifest compatibility: `>= 1.0.0`

## Summary

Distribution and instrumentation: the `brainfactory` CLI now publishes itself to
PyPI and npm on every release (OIDC Trusted Publishing, with provenance), the
framework reports a read-only metrics snapshot, and the diagram convention moves
to live-rendered Mermaid (SVG companions retired). See the
[CHANGELOG](../../../CHANGELOG.md) for the full list.

## Core modules

| Module | Change | Notes |
| --- | --- | --- |
| `operating-contract` | unchanged | |
| `session-ritual-hooks` | unchanged | |
| `continuity-log` | unchanged | |
| `capabilities-map` | unchanged | |
| `docs-mesh` | unchanged | |
| `decision-board` | unchanged | |
| `core-commands` | unchanged | |

## Action for brains

This release is hub-level — distribution, instrumentation, and documentation —
with no core-module content delta. Run `<prefix>-upgrade` (dry-run, then
`--apply`) to stamp `framework_version` 0.3.0 and refresh `synced_from`, preserving
extensions. The release also publishes the `brainfactory` CLI (0.1.1) to PyPI and
npm.
