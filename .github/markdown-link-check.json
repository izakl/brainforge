{
  "ignorePatterns": [
    {
      "pattern": "^http://localhost"
    },
    {
      "pattern": "^https://localhost"
    },
    {
      "pattern": "^mailto:"
    },
    {
      "comment": "Internal repo cross-references (bare repo URL or any path); validity is guaranteed by repo structure (and unauthenticated checks 404 on the private source repo).",
      "pattern": "^https://github\\.com/izakl/(brain-factory|brainforge)(/|$)"
    }
  ],
  "retryOn429": true,
  "retryCount": 3,
  "fallbackRetryDelay": "30s",
  "aliveStatusCodes": [200, 206, 429]
}
